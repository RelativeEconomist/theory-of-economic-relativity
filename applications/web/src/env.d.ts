/// <reference types="astro/client" />

// Injected by astro.config.mjs's `vite.define` from the repo root VERSION
// file, so the version string has exactly one source of truth.
declare const __TER_VERSION__: string;
